apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.k8s.yaml
  - service.k8s.yaml

configMapGenerator:
  - name: coredns-config
    files:
      - ./resources/Corefile
      - ./resources/i.f4n.dev.zone

